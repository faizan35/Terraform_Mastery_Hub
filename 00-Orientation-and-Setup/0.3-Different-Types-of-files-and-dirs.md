# 0.3 - Terraform Files & Directories

### Core Configuration Files

- **`main.tf`**

  - Contains the primary resource definitions (the “what to build”).
  - Can be split into multiple `.tf` files; Terraform loads all of them.

- **`variables.tf`**

  - Declares input variables (name, type, default, description).
  - Makes modules and root configs reusable and parameterized.

- **`outputs.tf`**

  - Declares values to expose after apply (e.g., VPC ID, IPs).
  - Useful for chaining between modules or printing results.

- **`versions.tf`**

  - Specifies Terraform CLI and provider version constraints.
  - Helps prevent version drift across teams.

- **`provider.tf`** _(optional but common)_

  - Configures providers (AWS, Azure, GCP, Kubernetes, etc.).
  - Example: region, credentials, aliases.

- **`locals.tf`** _(optional)_

  - Stores reusable values/expressions.
  - Helps keep code DRY (no repeated strings or logic).

- **`backend.tf`** _(optional)_

  - Defines the backend (where state is stored: S3, GCS, Azure Blob, etc.).
  - Can also be inline in `main.tf`, but often kept separate.

### Variable & Environment Handling

- **`terraform.tfvars`**

  - Default variable values (auto-loaded).

- **`*.auto.tfvars`**

  - Also auto-loaded; often used for environment-specific configs.

- **Custom `.tfvars` files**

  - Loaded manually with `-var-file=dev.tfvars` for different environments.

- **`.env`** _(not native, but used with `TF_VAR_` environment vars)\_

  - Helps load environment values into Terraform variables.

### State & Lock Files

- **`terraform.tfstate`**

  - JSON file that maps Terraform resources → real cloud resources.
  - **Do not edit manually.** Store remotely for teams.

- **`terraform.tfstate.backup`**

  - Auto-backup of the last state. Safety net if current state is corrupted.

- **`terraform.tfplan`** _(optional, generated)_

  - Binary file when you run `terraform plan -out=tfplan`.
  - Apply it later with `terraform apply tfplan`.
  - Not human-readable, usually ignored in Git.

- **`.terraform.lock.hcl`**

  - Dependency lock file (exact provider versions).
  - Ensures reproducible builds across machines and CI/CD.

- **`.terraform.tfstate.lock.info`** _(ephemeral)_

  - Temporary lock file created during `plan/apply` to prevent concurrent state edits.
  - Removed automatically when the run completes.

- **`.terraform/` directory**

  - Internal working dir created by `terraform init`.
  - Stores provider binaries, module downloads, plugin cache.
  - Should **not** be committed to Git.

### Metadata & Documentation

- **`README.md`**

  - Documentation for the module or project.
  - Explains usage, inputs, outputs, and examples.

- **`examples/` directory**

  - Contains sample usage of the module for quick adoption.

- **`modules/` directory**

  - Local reusable child modules.
  - Each subfolder contains its own `main.tf`, `variables.tf`, `outputs.tf`, etc.

- **`sentinel/` directory** _(Enterprise only)_

  - Stores Sentinel policies for policy-as-code (governance checks).

### Other Helpful Files

- **`.terraformrc` / `terraform.rc`**

  - User-level CLI config file.
  - Configures plugin cache dirs, credentials, provider mirrors.
  - Linux/Mac → `~/.terraformrc`
  - Windows → `%APPDATA%\terraform.rc`

- **`crash.log`** _(generated on crash)_

  - Terraform debug logs when a run fails unexpectedly.
  - Useful for troubleshooting and support.

- **`override.tf` / `*_override.tf`** _(rare)_

  - Overrides resource settings without touching original files.
  - Mostly for advanced/test scenarios.

- **`*.tf.json`**

  - JSON format of Terraform config (instead of HCL).
  - Useful when configs are generated programmatically.

- **`override.tf.json`**

  - JSON version of override files (rare).

- **`terraform.d/` (user home dir)**

  - Stores global Terraform data (credentials, cached plugins).
  - Different from `.terraform/` which is per-project.

- **`overrides/` directory (Terraform Enterprise/Cloud)**

  - Used for custom overrides in enterprise setups. Rare in OSS use.

### Typical Project Layout

```
project/
├── main.tf
├── variables.tf
├── outputs.tf
├── versions.tf
├── provider.tf
├── backend.tf
├── terraform.tfvars
├── modules/
│   └── vpc/
│       ├── main.tf
│       ├── variables.tf
│       └── outputs.tf
├── examples/
│   └── simple-usage/
│       └── main.tf
├── .terraform/                (generated)
├── terraform.tfstate          (generated)
├── terraform.tfstate.backup   (generated)
├── terraform.tfplan           (generated if -out is used)
├── .terraform.lock.hcl
├── crash.log                  (generated if crash happens)
└── .gitignore
```

### Interview Shortcut

_“Terraform projects use `.tf` files for config, `.tfvars` for variable values, `tfstate` to track real infra, `.terraform/` for provider binaries and modules, and `.terraform.lock.hcl` for provider version locking. Rare files like `terraform.tfplan`, `override.tf`, or `_.tf.json` exist but are less common.”\*

## Which files gets commited and which dose not

Excellent — this is something interviewers often check because it mixes **Terraform knowledge** with **Git best practices**.
Here’s a clear **commit vs ignore** breakdown for Terraform files and directories:

# Commit vs Ignore in Terraform Projects

| File / Directory                                                                                 | Purpose                                         | Commit to Git?                  |
| ------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ------------------------------- |
| `main.tf`, `variables.tf`, `outputs.tf`, `versions.tf`, `provider.tf`, `locals.tf`, `backend.tf` | Core configuration & logic                      | ✅ Yes                          |
| `terraform.tfvars`                                                                               | Default variable values (often non-sensitive)   | ⚠️ Maybe\*                      |
| `*.auto.tfvars`                                                                                  | Auto-loaded variables                           | ⚠️ Maybe\*                      |
| Custom `dev.tfvars`, `prod.tfvars`                                                               | Env-specific variables                          | ⚠️ Maybe\*                      |
| `.env`                                                                                           | Env variables (secrets, creds)                  | ❌ No                           |
| `README.md`                                                                                      | Documentation                                   | ✅ Yes                          |
| `examples/`                                                                                      | Example usage                                   | ✅ Yes                          |
| `modules/`                                                                                       | Local child modules                             | ✅ Yes                          |
| `terraform.tfstate`                                                                              | Infra state mapping (contains sensitive data)   | ❌ No                           |
| `terraform.tfstate.backup`                                                                       | Backup of state                                 | ❌ No                           |
| `.terraform.lock.hcl`                                                                            | Provider version lockfile (for reproducibility) | ✅ Yes                          |
| `.terraform/` directory                                                                          | Providers, modules cache (local only)           | ❌ No                           |
| `terraform.tfplan`                                                                               | Saved binary plan file                          | ❌ No                           |
| `crash.log`                                                                                      | Debug info if Terraform crashes                 | ❌ No                           |
| `override.tf`, `*_override.tf`                                                                   | Overrides for testing                           | ❌ No (temporary use only)      |
| `*.tf.json`, `override.tf.json`                                                                  | JSON configs/overrides                          | ✅ Yes (if intentional configs) |
| `.terraformrc` / `terraform.rc`                                                                  | User CLI config (cache dir, creds)              | ❌ No                           |
| `sentinel/` (Enterprise only)                                                                    | Policy-as-code                                  | ✅ Yes                          |
| `.gitignore`                                                                                     | Git ignore rules                                | ✅ Yes                          |

---

### Key Notes

- **✅ Commit** → Anything that defines infra logic, module code, docs, or lockfiles.
- **❌ Do Not Commit** → Anything with sensitive data (state, tfvars with secrets, env files, crash logs).
- **⚠️ Maybe** → Variable files (`*.tfvars`) can be committed _if they only hold non-sensitive defaults_. For secrets (passwords, keys, tokens), **never commit** — instead use `TF_VAR_` env vars or secret managers.

---

👉 Interview Shortcut Answer:
_“We commit `.tf` configs, module code, README, and `.terraform.lock.hcl`. We never commit state files, the `.terraform/` directory, plan files, or secret-containing `.tfvars`. Sensitive values belong in secret managers or CI/CD vars, not in Git.”_
